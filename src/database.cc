#include <unordered_map>
#include <vector>
#include "database.h"

using namespace std;

void Database::put(string key, string value)
{
}

std::string Database::get(string key)
{
}
void Database::remove(string key)
{
}
